{
    "name": "whereabouts",
    "version": "1.0.0",
    "description": "Natural-language location/time/weather Gutenberg block",
    "scripts": {
        "build": "wp-scripts build src/index.js --output-path=build",
        "start": "wp-scripts start src/index.js --output-path=build"
    },
    "devDependencies": {
        "@wordpress/scripts": "^27.0.0"
    }
}
