{
    "name": "whereabouts",
    "version": "1.4.0",
    "description": "Natural-language location/time/weather Gutenberg block and shortcode suite",
    "scripts": {
        "build": "wp-scripts build src/index.js --output-path=build",
        "start": "wp-scripts start src/index.js --output-path=build"
    },
    "devDependencies": {
        "@wordpress/scripts": "^27.0.0"
    }
}
